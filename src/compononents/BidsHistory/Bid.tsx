import React from "react";
import { BidType } from "../../Types"
import { Flex, Box } from "@chakra-ui/react";


export const Bid : React.FC<BidType> = ({valeur, par, date}) => {
    return (
        <Flex>

        </Flex>
    )
}